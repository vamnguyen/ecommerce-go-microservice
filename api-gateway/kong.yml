_format_version: "3.0"

services:
  - name: auth-service
    url: http://host.docker.internal:9001
    routes:
      - name: auth-service-routes
        strip_path: false
        paths:
          - /api/v1/auth
